<% if !@subscription.errors.present? %>
    $("#question-<%= @question.id %>").find('.subscription').html('<%= j render 'shared/subscription', question: @question, subscription: @subscription %>');
<% end %>
